import RC4.RC4;

public class Main {
    public static void main(String[] args) {
        System.out.println("Hello, World!");

        // Test de la classe RC4
        /*
        String texte = "test";
        String cle = "maSuperCleDeFou";

        // Convertir le texte et la clé en tableau de bytes
        byte[] texteBytes = texte.getBytes(); // Convertir le texte en tableau de bytes
        byte[] cleBytes = cle.getBytes(); // Convertir la clé en tableau de bytes

        // Créer une instance de RC4 avec la clé
        RC4 rc4 = new RC4(cleBytes); // Initialiser RC4 avec la clé

        // Chiffrer le texte
        byte[] texteChiffre = rc4.chiffrer(texteBytes); // Chiffrer le texte
        System.out.println("Texte chiffré : " + new String(texteChiffre)); // Afficher le texte chiffré

        // Déchiffrer le texte en utilisant la même clé
        RC4 rc4Decrypt = new RC4(cleBytes); // Re-initialiser RC4 pour le déchiffrement avec la même clé
        byte[] texteDechiffre = rc4Decrypt.chiffrer(texteChiffre); // RC4 est symétrique donc on peut déchiffrer avec la même clé
        System.out.println("Texte déchiffré : " + new String(texteDechiffre)); // Afficher le texte déchiffré
        */

    }
}